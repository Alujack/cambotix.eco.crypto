# Optional NVIDIA GPU passthrough for the Docker Ollama profile (Windows WSL2 or Linux hosts with the NVIDIA driver).
# Enable by adding to .env:  COMPOSE_FILE=compose.yaml:compose.gpu.yaml
services:
  ollama:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
